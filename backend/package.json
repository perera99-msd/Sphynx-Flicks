{
  "name": "movie-app-backend",
  "version": "1.0.0",
  "description": "Backend for Movie Discovery App",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute movie-app-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}